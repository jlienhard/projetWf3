<form method="post" action="{{ path('admin_membre_delete', {'id': membre.id}) }}" onsubmit="return confirm('Confirmez-vous la suppression ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membre.id) }}">
    <button class="btn">Supprimer</button>
</form>
