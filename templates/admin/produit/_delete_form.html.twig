<form method="post" action="{{ path('admin_produit_delete', {'id': produit.id}) }}" onsubmit="return confirm('Confirmez-vous la suppression ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.id) }}">
    <button class="btn">Supprimer</button>
</form>
